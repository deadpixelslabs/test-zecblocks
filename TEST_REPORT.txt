Pending NFT claim recovery — 2026-09-22

Production records checked at 10:42–10:45 UTC:
- Screenshot claim #774 has a protocol-audited, chain-confirmed claim for the screenshot wallet. Its transaction is 607a28eefce5b4e48f8e48718dd147c0b8e4f5f14ee4df81a0ce877ca36c34dc.
- #3874 was canonically clear with no indexed CLAIM event. This does not establish whether the wallet attempted a broadcast; its browser journal and Noir transaction history are not accessible here.
- Runtime-log access through Vercel was unavailable (403). Diagnosis uses the checked-in recovery code and read-only production indexer records. No database rows were manually changed.

Previously, one click checked only one queued NFT, the single notice obscured other results, missing-TXID recovery returned before checking canonical settlement, and wallet-history/audit errors could appear as indefinite waiting.
Recovery now checks every queued claim with two workers, checks canonical settlement first, shows each stage/result separately, reports wallet/verifier errors, and releases controls after a 60-second check deadline. Guards prevent late results from changing another session. Ambiguous journals are retained; no repeat payment/signature is initiated.

The suite contains 35 Node/Chromium tests. New regression coverage includes the two-claim screenshot scenario, confirmed claims without a local TXID, exact-event registration, history timeouts, a hung indexer with late completion, account changes and explicit audit errors. Existing mining, minting, canonical-counter and mobile-layout coverage is retained. Added recovery screenshots for desktop and mobile.
Local syntax, whitespace and four non-browser checks passed. Local Chromium download failed, so the full browser suite runs in the repository's GitHub Actions workflow before production promotion; that commit's CI result is authoritative. The production check also requires the new recovery markup and queue handler.
Tests use synthetic wallet/API fixtures. No real Noir transaction approval or mainnet payment was executed.

Previous scan progress source correction — 2026-09-21

The production scanner cursor was 4,901 (98%) while the user saw 0%. zecblocks_mining_snapshot is an invoker function; its indexer-state read is filtered by RLS (enabled, no anon policies), so it falls back to cursor 1. The live-stats Edge Function can read the real cursor, but the next public snapshot overwrote it in the browser.
The UI now accepts scan cursor/completion only from live-stats, preserving private indexer RLS. Before that source responds, it shows Checking scan progress instead of an assumed 0%. Genuine scan-cycle restarts still update to 0%. Claim totals and the four-relay availability requirement are unchanged.
The regression suite covers live 98% followed by a newer public snapshot returning cursor 1, then a genuine live restart. The production check now also validates the live-stats cursor response.
All 28 local Node/Chromium tests passed, along with inline syntax and whitespace checks.

Previous community progress display — 2026-09-21

Claims seen is restored to the main collection statistics, with a progress bar and percentage of the 5,000 supply. The confirmed count remains separately visible below it. Both follow the existing server refresh, cached data and stale-response protection; no total is hardcoded and no mining attempt increments the UI locally.
Production statistics at inspection: 3,551 claims seen (71.02%) and 2,787 confirmed. These are different metrics, with the explanation retained in details.
All 27 existing regression tests passed. Desktop dark and mobile light screenshots were reviewed; the fixture correctly shows 3,505 claims seen / 70.10% and 2,660 confirmed. Existing mobile overflow and stale-response tests passed. Production read check now also requires the restored progress-bar markup.

Previous claim counter and audit queue investigation — 2026-09-21

Production evidence:
- At 21:38–21:39 UTC, claims_seen stayed 3,551 while canonical availability rose from 2,738 to 2,740.
- zecblocks_claims_seen_live uses the greatest historical/observed unique-ID count, including events without a validity filter. 761 available IDs had already been observed more than six hours earlier. A valid new claim on those IDs need not increase the historical counter.
- At 21:45 UTC, all 30 selected audit transactions were deferred retries. Another 42 signature-verified but protocol-unaudited transactions and 3 pending transactions were outside the batch; some had waited since September 19. The final ORDER BY updated_at DESC repeatedly favored retries whose failure refreshed updated_at.
- No canonical-clear ID overlapped a protocol-audited, chain-confirmed, verified CLAIM in the database checks. The deployed finder checks relay quorum and availability before/after reservation. This is evidence about current indexed state, not proof that every wallet or future race will succeed.
- Applied fair_claim_audit_queue at 21:49 UTC. Oldest outstanding work now runs first; finalized transaction duplicates are excluded. Existing function ACL remains postgres + service_role only. Claim validation, chain rules and historical counters are unchanged.
- At 21:50:26 UTC, canonical claims were 2,787, claims_seen remained 3,551, and only four older untouched items remained in the selected 40-item queue. Background validation, rather than manual count changes, produced these results. Totals continue changing with live activity.

Verification:
- 27/27 local Node/Chromium regression tests passed, including a confirmed-count increase with unchanged historical count, stale responses and failed reads.
- Read-only PostgreSQL regression generated from the actual migration passed all four checks: older claims get selected, 30 unique transactions per batch, richer duplicate payload preferred, finalized claims protected.
- Inline script syntax and git diff whitespace checks passed.
- Main UI now shows Confirmed claims; Claims seen is retained with a historical-count explanation in details. Production read check requires both displays and checks the canonical count field.
- Existing external transactions were inspected through production indexer records; no real wallet transaction was initiated. Direct explorer access from this environment timed out. Some transactions may remain deferred until chain data is available.

Previous mining interface verification

Local regression suite: 26/26 passed. Syntax and DOM ID/reference checks passed.
Existing CPU proof, single-broadcast, account change, fee gate and recovery fixtures remain active.
New coverage verifies gallery source artwork, search, pagination and selection; browsing performs no reservation or payment. Tab navigation preserves active mining, disables ZECS mint while mining, and restores #zecs on reload.
Desktop and mobile screenshots cover both NFT and ZECS panels in dark and light themes. Mobile overflow is checked at 390px.
Tests use synthetic wallet/API fixtures and reduced proof difficulty. No real-wallet transaction or mainnet payment was executed.

Previous recovery verification

Mining Studio V16.1.1 recovery regression verification

Local verification: 24 Node/Chromium regression tests, using Playwright 1.55.1.
The current GitHub Actions run is the authoritative CI result for its commit.

Coverage includes the original 12 tests plus:
- 52 and 125 historical ZECS transactions under the real 50-TXID lookup limit.
- Automatic and manual cleanup of already registered discovery locks, with zero sends/signatures.
- A foreign owner registration cannot clear this wallet's lock.
- Legacy NFT lock migration preserves #3874 while a different ID can mine and claim once.
- Exact-event NFT backfill bypasses historical backlog, and canonical settlement releases the lock.
- Targeted claim history skips unrelated NFT chain lookups.
- Hung wallet reads release action gates without losing pending transactions.
- A late broadcast after timeout/account change is saved for the original owner.
- Partial ZECS registration failures preserve all queued TXIDs.
- Transport cancellation is not classified as explicit wallet rejection.
- Slow NFT indexing does not hold the wallet action gate.

Read-only production evidence on 2026-09-21:
The screenshot wallet had 52 confirmed ZECS mints. The deployed lookup function accepts
only 50 TXIDs per call; the old frontend sent up to 60 and falsely treated omitted rows as missing.
NFT IDs 3874 and 1900 were canonically clear at inspection. The user's browser-local
pending transaction data cannot be inspected from this environment.

No real Noir transaction approval/payment was performed. Fixtures do not prove real
Zcash settlement or hardware GPU performance. Production proof difficulty, supply,
fee mode, holder eligibility and relay checks are unchanged. No database rows were edited.
Live verification also found a resource-limit failure from browser-triggered full scans.
The existing active once-per-minute availability cron was verified, and redundant public-page
scan triggers were removed. The startup fixture now asserts zero full-scan requests.
